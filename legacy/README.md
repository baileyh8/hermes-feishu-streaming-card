# Legacy Archive

This directory contains historical implementations, early scripts, old screenshots, and migration references that are not part of the V3.0.0 active runtime.

Active V3.0.0 code lives in:

- `hermes_feishu_card/`
- `tests/`
- `docs/`
- `config.yaml.example`

Do not use files in this directory for new installations. They are kept only for historical comparison, migration notes, and recovery context.
